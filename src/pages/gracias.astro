---
import Layout from "../layouts/Layout.astro";
---

<Layout title="¡Gracias! | ClearHome">
    <!-- Event snippet for Enviar formulario de clientes potenciales conversion page -->
    <script is:inline>
        gtag("event", "conversion", {
            send_to: "AW-17833674787/jvdoCILzttwbEKOQ4bdC",
        });
    </script>
    <div
        class="min-h-screen flex items-center justify-center bg-gray-50 relative overflow-hidden"
    >
        <!-- Confetti Background (CSS only for simplicity or lightweight script if needed later) -->
        <div class="absolute inset-0 pointer-events-none overflow-hidden">
            <div
                class="absolute top-0 left-1/4 w-3 h-3 bg-brand-accent rounded-full animate-fall opacity-0"
                style="animation-delay: 0s;"
            >
            </div>
            <div
                class="absolute top-0 left-1/2 w-4 h-4 bg-brand-blue rounded-full animate-fall opacity-0"
                style="animation-delay: 1s;"
            >
            </div>
            <div
                class="absolute top-0 left-3/4 w-3 h-3 bg-yellow-400 rounded-full animate-fall opacity-0"
                style="animation-delay: 2s;"
            >
            </div>
            <div
                class="absolute top-0 left-1/3 w-2 h-2 bg-green-400 rounded-full animate-fall opacity-0"
                style="animation-delay: 1.5s;"
            >
            </div>
        </div>

        <div
            class="bg-white p-10 rounded-3xl shadow-2xl max-w-lg w-full text-center relative z-10 mx-4 animate-fade-in-up"
        >
            <div
                class="w-24 h-24 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-8 animate-bounce-slow"
            >
                <i class="fas fa-check text-5xl text-green-500"></i>
            </div>

            <h1 class="text-4xl font-black text-brand-dark mb-4">
                ¡Mensaje Recibido!
            </h1>

            <p class="text-gray-600 text-lg mb-8 leading-relaxed">
                Gracias por confiar en <span class="font-bold text-brand-blue"
                    >ClearHome</span
                >. Hemos recibido tu solicitud correctamente.
                <br /><br />
                Nuestro equipo está revisando tus detalles y te contactaremos en breve
                con tu presupuesto personalizado.
            </p>

            <a
                href="/"
                class="inline-flex items-center gap-2 bg-brand-blue text-white font-bold py-4 px-8 rounded-xl hover:bg-brand-dark transition shadow-lg hover:-translate-y-1"
            >
                <i class="fas fa-arrow-left"></i> Volver al Inicio
            </a>

            <p class="mt-8 text-xs text-gray-400">
                Te hemos enviado una copia de la confirmación a tu correo.
            </p>
        </div>
    </div>
</Layout>

<style>
    @keyframes fall {
        0% {
            transform: translateY(-10vh) rotate(0deg);
            opacity: 1;
        }
        100% {
            transform: translateY(100vh) rotate(360deg);
            opacity: 0;
        }
    }
    .animate-fall {
        animation: fall 4s linear infinite;
    }

    @keyframes fade-in-up {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
    .animate-fade-in-up {
        animation: fade-in-up 0.8s ease-out forwards;
    }

    @keyframes bounce-slow {
        0%,
        100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-10px);
        }
    }
    .animate-bounce-slow {
        animation: bounce-slow 2s infinite ease-in-out;
    }
</style>
